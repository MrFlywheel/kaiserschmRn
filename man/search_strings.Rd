\name{search_strings}
\alias{search_strings}
%- Crop point clouds with polygons
\title{
Search (multiple) expressions in character vectors
}
\description{
As many expressions as wanted can be searched for wothin each element of a vector containing character elements. Matching elements will be returned.
}
\usage{
search_strings(data, expr=NULL)
}

\arguments{
  \item{data}{
An vector of character elements
} \item{expr}{
A vector containing character expressions to search for.
}
}
\details{
%%  ~~ If necessary, more details than the description above ~~
}
\value{
The function searches (multiple) expressions in character vectors
}
\references{
%% ~put references to the literature/web site here ~
}
\author{
MrFlywheel
}
\note{
%%  ~~further notes~~
}

%% ~Make other sections like Warning with \section{Warning }{....} ~

\seealso{
%% ~~objects to See Also as \code{\link{help}}, ~~~
}
\examples{
##---- Should be DIRECTLY executable !! ----
##-- ==>  Define data, use random,
##--	or do  help(data=index)  for the standard data sets.

## The function is currently defined as
function (x)
{
  }
}
% Add one or more standard keywords, see file 'KEYWORDS' in the
% R documentation directory (show via RShowDoc("KEYWORDS")):
% \keyword{ ~kwd1 }
% \keyword{ ~kwd2 }
% Use only one keyword per line.
% For non-standard keywords, use \concept instead of \keyword:
% \concept{ ~cpt1 }
% \concept{ ~cpt2 }
% Use only one concept per line.
